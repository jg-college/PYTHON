import numpy as np

mat = np.array([
    [10, 20, 30],
    [40, 50, 60],
    [70, 80, 90]
])
print(np.sum(mat)) #mat.sum()
print(np.sum(mat,axis=0)) #column-wise
print(np.mean(mat,axis=1)) #row-wise
print(np.max(mat,axis=1))
print(np.std(mat,axis=0))
